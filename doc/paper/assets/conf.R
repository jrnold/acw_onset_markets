source("../../conf.R", chdir=TRUE)
library("ggplot2")
library("xtable")

theme_local <- function(base_size = 8) {
  theme_gray(base_size = base_size)
}

WIDTH <- 5
HEIGHT <- 3

WIDTH_FULL <- 5.5
HEIGHT_FULL <- 7

